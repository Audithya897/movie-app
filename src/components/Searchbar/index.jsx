import "./style.css"
const Searchbar = () => {
    return (
        <section className="SearchBar">
            <input placeholder="Search movies..." />
        </section>
    );
}

export default Searchbar